import styled from 'styled-components'

export const Btn = styled.button`
  background-color: var(--blue-color);
  padding: 15px 37px;
  min-width: 166px;

  text-align: center;
  font-family: inherit;
  font-style: normal;
  font-weight: normal;
  font-size: 15px;
  line-height: 30px;
  color: white;

  border: none;
  border-radius: 3px;

  &:hover {
    box-shadow: 0px 0px 6px 1px rgba(82, 192, 255, 0.5);
  }

  cursor: pointer;
`
